# Quest Journal

> ⚠️ **Spoiler:** contains this save's full story progress.
> Auto-generated from the savegame; do not edit by hand (rebuild with `tools/build_journal.py`).

- **Save:** Campsite - 98h 44m
- **Game time (internal):** 361260
- **Open / in progress:** 40  ·  **Completed:** 72

## 🟡 Open / in-progress quests

### Prologue — Interlude

<details class="quest-entry"><summary><code>INT_EmperorRevealed_COMPLETION</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Current objective: <code>INT_EmperorRevealed_COMPLETION</code></li>
<li>Trail: <code>IntWoke</code> → <code>IntInsideAP</code> → <code>IntOrpheusFight</code> → <code>IntSceneComplete</code></li>
</ul>
</details>

### Act 1 — Grymforge

<details class="quest-entry"><summary><code>FOR_DangerousBook_SUB_EvilTome_DestroyTome</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Current objective: <code>FOR_DangerousBook_SUB_EvilTome_DestroyTome</code></li>
<li>Trail: <code>DestroyTome</code> → <code>DestroyTome_Radiant</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>FOR_DangerousBook_SUB_UseTome_FindKey</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Current objective: <code>FOR_DangerousBook_SUB_UseTome_FindKey</code></li>
<li>Trail: <code>KeyLost</code> → <code>FindKey</code> → <code>UnlockTome</code> → <code>FinishTome</code> → <code>InvestigateMissingKnowledge</code> → <code>FindWayToDecypherPage</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>FOR_DangerousBook_UnlockLastPage</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Current objective: <code>FOR_DangerousBook_UnlockLastPage</code></li>
<li>Trail: <code>FindCellar</code> → <code>SearchLabAfterJournal</code> → <code>FindTome</code> → <code>InvestigateTome</code> → <code>MadnessKnowledge</code> → <code>ReachedLastPage</code></li>
</ul>
</details>

### Act 2 — Shar questline

<details class="quest-entry"><summary><code>SHA_Nightsong</code><span class="q-count">13 steps</span></summary>
<ul>
<li>Current objective: <code>SHA_Nightsong_AfterSCE</code></li>
<li>Trail: <code>SearchTemple</code> → <code>LearnedLocation</code> → <code>HeardOf</code> → <code>LearnedBryanMap</code> → <code>SolvedPuzzle</code> → <code>ReadJusticiarBook</code> → <code>EnteredUnderdark</code> → <code>ReachedGrymforge</code> → <code>FoundSHA</code> → <code>BalthazarSentForNightsong</code> → <code>EnteredShadowfell</code> → <code>NightsongIsAPersonPrison</code> → <code>NightsongInCamp</code></li>
</ul>
</details>

### Act 3 — Rivington / Wyrm's Crossing

<details class="quest-entry"><summary><code>WYR_CazadorPalace_ReachPalace</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>WYR_CazadorPalace_ReachPalace</code></li>
<li>Trail: <code>GotGurQuest</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>WYR_Clown_FindParts</code><span class="q-count">7 steps</span></summary>
<ul>
<li>Current objective: <code>WYR_Clown_FindParts</code></li>
<li>Trail: <code>GotQuest_DoesntKnow</code> → <code>LearnedDead_SwD</code> → <code>LearnedSplit_SwD</code> → <code>GotTorso</code> → <code>LearnedSplit</code> → <code>FoundHand</code> → <code>GotHand</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>WYR_Donations_InestigateFireworks</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Current objective: <code>WYR_Donations_InestigateFireworks</code></li>
<li>Trail: <code>FoundToys_DoesntKnow</code> → <code>FoundBlackmail_After</code> → <code>LearnedPassword_ArfurConfessed</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>WYR_FindWayIntoWyrmRock_EnterFortress</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Current objective: <code>WYR_FindWayIntoWyrmRock_EnterFortress</code></li>
<li>Trail: <code>GotCoronationPass</code> → <code>GotSecretEntranceMap</code> → <code>GotLowerCityPass</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>WYR_GetGortashGem_LearnGortashLocation</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Current objective: <code>WYR_GetGortashGem_LearnGortashLocation</code></li>
<li>Trail: <code>GotKethericGem</code> → <code>EnteredWyrmsCrossing</code> → <code>HadSouthCheckpointScene</code></li>
</ul>
</details>

### Act 3 — Lower City

<details class="quest-entry"><summary><code>LOW_FindBhaalTemple_SUB_BecomeUnholyAssassin</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>LOW_FindBhaalTemple_SUB_BecomeUnholyAssassin</code></li>
<li>Trail: <code>UnholyAssasin_Start</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>LOW_FindBhaalTemple_SUB_StopMurdersDevella</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Current objective: <code>LOW_FindBhaalTemple_SUB_StopMurdersDevella</code></li>
<li>Trail: <code>StopMurders_ValeriaTargetList</code> → <code>StopMurders_ValeriaSendUsToDevella</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>LOW_FindBhaalTemple_Start</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Current objective: <code>LOW_FindBhaalTemple_Start</code></li>
<li>Trail: <code>LearnedAboutOrin</code> → <code>EnteredCity</code> → <code>OrinImpersonatedFirst</code> → <code>FindBhaalTemple_TableauxOfMurder</code> → <code>BecomeOrFollowMurders</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>LOW_FindMol_MolNotInColony</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>LOW_FindMol_MolNotInColony</code></li>
<li>Trail: <code>MolNotInColonyDefeatedKetheric</code></li>
</ul>
</details>

### Act 3 — City (misc)

<details class="quest-entry"><summary><code>PLA_GrandDukeRescue</code><span class="q-count">9 steps</span></summary>
<ul>
<li>Current objective: <code>PLA_GrandDukeRescue_PursueGrandDuke</code></li>
<li>Trail: <code>LearnedMoonrise</code> → <code>OrderedBreachInn</code> → <code>EnteredInn</code> → <code>FoundStuckHalfElf</code> → <code>FreedCounsellor</code> → <code>LearnedDukeMissingCounsellor</code> → <code>Counsellor_AcceptedHelp_Moonrise</code> → <code>ReachedMT</code> → <code>Tadpoled</code></li>
</ul>
</details>

### Main quest (cross-act)

<details class="quest-entry"><summary><code>GLO_DealWithTheBrain_DestroyTheBrain</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>GLO_DealWithTheBrain_DestroyTheBrain</code></li>
<li>Trail: <code>LearnedOfDestroyOption</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_DealWithTheBrain_DominateTheBrain</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>GLO_DealWithTheBrain_DominateTheBrain</code></li>
<li>Trail: <code>LearnedOfDominateOption</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_DealWithTheBrain_FindNetherStones</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Current objective: <code>GLO_DealWithTheBrain_FindNetherStones</code></li>
<li>Trail: <code>LearnedOfElderBrain</code> → <code>ArrivedInBaldursGate</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_FreeOrpheus_OrphicHammer</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Current objective: <code>GLO_FreeOrpheus_OrphicHammer</code></li>
<li>Trail: <code>DiscussedOrpheus</code> → <code>HaveVossDirections</code> → <code>LearnedAboutHammer</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_GatherYourAllies_AvailableAllies</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Current objective: <code>GLO_GatherYourAllies_AvailableAllies</code></li>
<li>Trail: <code>VoloPromisesSupport</code> → <code>DaisyAdvisesAid</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_HelpVoss_RetrieveHammer</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Current objective: <code>GLO_HelpVoss_RetrieveHammer</code></li>
<li>Trail: <code>AgreeToVoss</code> → <code>TalkedToLaezelAboutOrpheus</code> → <code>EnteredBGO</code> → <code>SpeakToRaphael</code> → <code>AfterRaphaelNoDeal</code> → <code>SpokeToVossInSC</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_RaphaelDeal_MakeDeal</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Current objective: <code>GLO_RaphaelDeal_MakeDeal</code></li>
<li>Trail: <code>GotMamzellInvitation</code> → <code>GotKorrillaInvitation</code> → <code>TalkedToRaphael</code> → <code>RefusedDeal</code></li>
</ul>
</details>

### Companion questlines

<details class="quest-entry"><summary><code>ORI_Avatar_Astarion_ReachPalace</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_Avatar_Astarion_ReachPalace</code></li>
<li>Trail: <code>MetRaphaelMasoleum</code> → <code>KilledOrhon</code> → <code>ScarsRevealed</code> → <code>ReachedBG</code> → <code>Ulma_GurPeace</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_Avatar_Gale_FindHeart</code><span class="q-count">7 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_Avatar_Gale_FindHeart</code></li>
<li>Trail: <code>NeedItem1</code> → <code>GotItem1</code> → <code>NeedItem2</code> → <code>GotItem2</code> → <code>NeedItem3</code> → <code>GotItem3</code> → <code>ElminsterBrief</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_Avatar_Laezel_GetOrphicHammer</code><span class="q-count">15 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_Avatar_Laezel_GetOrphicHammer</code></li>
<li>Trail: <code>ZorruMarkedMap</code> → <code>ResolvedPeaceCreche</code> → <code>FoundCrecheLocationNoHints</code> → <code>SawGithyankiAtGates_KnowsLocation</code> → <code>EnteredCreche</code> → <code>PermaDefeatInquisitor</code> → <code>BackToInquisitor_InquisitorDead</code> → <code>SpokeToVlaakith</code> → <code>FindCure_LeftRoomAfterVlaakithCommand</code> → <code>FindCure_AfterBetrayal</code> → <code>AgreedVoss</code> → <code>FinishedIntermezzo_VossInSC</code> → <code>SpeakToRaphael</code> → <code>LearnedAboutKeyFromRaphael_VossInSC</code> → <code>SpokeToVossInSC</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_Avatar_Shadowheart_FindHandler</code><span class="q-count">15 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_Avatar_Shadowheart_FindHandler</code></li>
<li>Trail: <code>IncurableWound_FirstFlare</code> → <code>VoiceOfAbsolute</code> → <code>AbsoluteBox</code> → <code>IncurableWound_Introspection</code> → <code>FoundJusticiarHint</code> → <code>CampFight_Peace</code> → <code>FoundGithPatrol</code> → <code>SeenBoxInsides</code> → <code>SharTemple</code> → <code>ConfessedSharran_Alt</code> → <code>SparedNightsong</code> → <code>EnemyOfShar_SharWrath</code> → <code>EnemyOfShar_NightsongMeeting</code> → <code>SeenWolfDream</code> → <code>EnemyOfShar_ArrivedInWYR</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_Avatar_Wyll_MeetingMizora</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_Avatar_Wyll_MeetingMizora</code></li>
<li>Trail: <code>KarlachKilled</code> → <code>MetPaladinsOfTyr</code> → <code>MizorasCaptureHappened</code> → <code>MizorasCaptureReachedColony</code> → <code>MizorasRescueHappenedPact</code> → <code>ReachedAct3</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_Avatar_Wyll_SUB_Duke_Pursue</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_Avatar_Wyll_SUB_Duke_Pursue</code></li>
<li>Trail: <code>LearntMT</code> → <code>InnReached</code> → <code>CouncillorTalkedKnowsMT</code> → <code>ReachedMT</code> → <code>Tadpoled</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Astarion_ReachPalace</code><span class="q-count">15 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_COM_Astarion_ReachPalace</code></li>
<li>Trail: <code>RecruitedAstarion</code> → <code>LearnedVampire_GaveBlood</code> → <code>MetGur_LearnedVampire_AlreadyKnew</code> → <code>MetGur_ToldCampLocation</code> → <code>ToldGur_CazadorSuspect</code> → <code>MetRaphaelMasoleum</code> → <code>KilledOrhon</code> → <code>ScarsRevealed</code> → <code>ToldOtherSpawns_INT</code> → <code>ReachedBG</code> → <code>ReachedBG_KnowSpawns</code> → <code>LearnedAstarionDeath</code> → <code>LearnedAstarionSlave</code> → <code>ToldAstarionFeed_Enemies</code> → <code>Ulma_GurPeace</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Gale_FindBook1</code><span class="q-count">13 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_COM_Gale_FindBook1</code></li>
<li>Trail: <code>RecruitedGale</code> → <code>LearnedGoal</code> → <code>NeedFirstItem</code> → <code>GaveOneItem</code> → <code>NeedSecondItem</code> → <code>GaveTwoItems</code> → <code>NeedThirdItem</code> → <code>GaveThreeItems</code> → <code>LearnedBackstory</code> → <code>MeetElminster</code> → <code>ElminsterBrief</code> → <code>GaleMissedBrain</code> → <code>FindBook_Raphael</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Laezel_GetOrphicHammer</code><span class="q-count">23 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_COM_Laezel_GetOrphicHammer</code></li>
<li>Trail: <code>RecruitedLaezel_Cage</code> → <code>FoundZorruLZAbsent</code> → <code>ZorruMarkedMap</code> → <code>LaezelRanToKnight</code> → <code>ResolvedPeaceCreche</code> → <code>FoundCrecheLocationNoHints</code> → <code>SawGithyankiAtGates</code> → <code>EnteredCreche</code> → <code>GainedAccess</code> → <code>PermaDefeatInquisitor</code> → <code>BackToInquisitor_InquisitorDead</code> → <code>SpokeToVlaakith_LaezelConvinced</code> → <code>VlaakithBetrayal_LeftRoomWithoutEntering</code> → <code>FindCure_AfterBetrayal</code> → <code>CrecheFinished</code> → <code>AgreedVoss</code> → <code>FinishedIntermezzo</code> → <code>TalkedToLaezelAboutOrpheus_VossInSC_OrpheusPath</code> → <code>VlaakithVisited_TalkedToLaezelAboutOrpheus_VossInSC_OrpheusPath</code> → <code>SpeakToRaphael</code> → <code>AfterRaphael_NoDeal</code> → <code>AfterRaphaelNoDeal_VossInSC_OrpheusPath</code> → <code>SpokeToVoss_OrpheusPath</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Shadowheart_GoToHoG</code><span class="q-count">14 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_COM_Shadowheart_GoToHoG</code></li>
<li>Trail: <code>Recruited_Beach</code> → <code>SawIncurableWound</code> → <code>IncurableWoundDiscussed</code> → <code>CampFight_Peace</code> → <code>LearnedMission_UnknowShar_UnknowBoxMission</code> → <code>SharTemple</code> → <code>IncurableWoundFlaredManyTimes</code> → <code>LearnedSharWorship_Alone</code> → <code>SparedNightsong</code> → <code>EnemyOfShar_SharWrath</code> → <code>EnemyOfShar_NightsongMeeting</code> → <code>EnemyOfShar_ArrivedInWYR</code> → <code>EnemyOfShar_LearnedCultLocation</code> → <code>Recruited_Laezel</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Shadowheart_SUB_Justiciar_COMPLETION</code><span class="q-count">17 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_COM_Shadowheart_SUB_Justiciar_COMPLETION</code></li>
<li>Trail: <code>JusticiarHint_Halsin_Update</code> → <code>EnteredKethericCity</code> → <code>HeardSharTemple</code> → <code>HeardSharTemplePathBlocked</code> → <code>FoughtFiends</code> → <code>JusticiarHint_TempleApproach_Update</code> → <code>FoundSharTemple_Alt</code> → <code>CompletedStealthTrial</code> → <code>CompletedCloneTrial</code> → <code>CompletedDarknessTrial</code> → <code>HadCommunion_NoSpear</code> → <code>FoundTrials</code> → <code>LootedSpearOfNight_Communion</code> → <code>CompletedTrials</code> → <code>EnteredShadowfell_Balthazar</code> → <code>BalthazarFight</code> → <code>Nightsong_Spared</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Wyll_MeetingMizora</code><span class="q-count">9 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_COM_Wyll_MeetingMizora</code></li>
<li>Trail: <code>RecruitedWyll</code> → <code>KarlachKilled</code> → <code>PostKarlachDead</code> → <code>MizorasJudgementReward</code> → <code>MetPaladinsOfTyr</code> → <code>MizorasCaptureHappened</code> → <code>MizorasCaptureReachedColony</code> → <code>MizorasRescueHappenedPact</code> → <code>ReachedAct3</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Wyll_SUB_Duke_Pursue</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Current objective: <code>ORI_COM_Wyll_SUB_Duke_Pursue</code></li>
<li>Trail: <code>LearntMT</code> → <code>InnReached</code> → <code>CouncillorTalkedKnowsMT</code> → <code>RavengardFather</code> → <code>ReachedMT</code> → <code>Tadpoled</code></li>
</ul>
</details>

### Hidden objectives

<details class="quest-entry"><summary><code>HIDDEN_BGO_Boosters_Objective</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>HIDDEN_BGO_Boosters_Objective</code></li>
<li>Trail: <code>WYR_NymphGrotto_Rapture</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>HIDDEN_CRE_Boosters_Objective</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>HIDDEN_CRE_Boosters_Objective</code></li>
<li>Trail: <code>CRE_WeaponsPuzzle_SolvedPuzzle</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>HIDDEN_SCL_Boosters_Objective</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Current objective: <code>HIDDEN_SCL_Boosters_Objective</code></li>
<li>Trail: <code>SCL_OliversShadow_WonHideAndSeek</code> → <code>SCL_NightsongPrison_Weapon</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>HIDDEN_TUT_Boosters_Objective</code><span class="q-count">1 step</span></summary>
<ul>
<li>Current objective: <code>HIDDEN_TUT_Boosters_Objective</code></li>
<li>Trail: <code>TUT_Lab_ExtractedIntDev</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>HIDDEN_WLD_Boosters_Objective</code><span class="q-count">21 steps</span></summary>
<ul>
<li>Current objective: <code>HIDDEN_WLD_Boosters_Objective</code></li>
<li>Trail: <code>ORI_Gale_Freed</code> → <code>DEN_Confrontation_PersuadedAradin</code> → <code>FOR_PriestOfTheConqueringGod_Commanded</code> → <code>DEN_GoblinScouts_SavedDruid</code> → <code>GLO_BookOfRestoredGods_Opened</code> → <code>DEN_GruntingKid_Persuaded</code> → <code>DEN_Thieflings_BefriendedMol</code> → <code>DEN_ElevatorBear_Moved</code> → <code>CAMP_Courier_Tamed</code> → <code>CRA_HarperBooster_Looted</code> → <code>FOR_GoblinAmbush_Persuaded</code> → <code>FOR_ThayanCellar_OpenMirror</code> → <code>FOR_Backpack_SucceededCheck</code> → <code>GOB_GoblinKing_Performed</code> → <code>GOB_Torturers_Freed</code> → <code>UND_SwordInStone_SucceededCheck</code> → <code>UND_ArcaneTower_Activated</code> → <code>PLA_Tavern_CalmedOx</code> → <code>PLA_GithChokepoint_Fooled</code> → <code>UND_DeadInTheWater_AcquiredTrinket</code> → <code>UND_HarperSpy_OpenedStash</code></li>
</ul>
</details>

## ✅ Completed quests

### Prologue — Nautiloid

<details class="quest-entry"><summary><code>TUT_NautiloidEscape</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>Start</code> → <code>LearnedHelm_Devourer</code> → <code>ReachedHelm</code> → <code>EscapedHell</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>TUT_ShadowheartEscape</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>FindCaptive</code> → <code>UsedRune</code> → <code>FreedAndRecruited</code></li>
</ul>
</details>

### Act 1 — Recruitment & Chapel

<details class="quest-entry"><summary><code>CHA_Chapel</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Trail: <code>ReadDruidBook</code> → <code>AccessedTemple</code> → <code>WeirdDoorEntrance</code> → <code>FoundSarcophagus_Sealed</code> → <code>OpenedSarcophagus</code> → <code>Exit</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>CHA_LaezelRecruit</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Trail: <code>Started</code> → <code>IntimidatedLeave</code> → <code>DidntAgreeHelp</code> → <code>LearnedCure</code> → <code>Freed</code> → <code>InvitedCamp</code></li>
</ul>
</details>

### Act 1 — Grove & Goblins

<details class="quest-entry"><summary><code>DEN_CapturedGoblin</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Trail: <code>MetGoblin</code> → <code>Released</code> → <code>Escaped</code> → <code>FoundCheckpoint</code> → <code>FoundNearDrow</code> → <code>Saved</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_Conflict</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>MetZevlor</code> → <code>RathGaveQuest_BeforeZev</code> → <code>MetKagha</code> → <code>KaghaDiedEarly</code> → <code>NoTieflings</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_Conflict_SUB_Goblin</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>Goblin_NoTieflings</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_Conflict_SUB_Halsin</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>LearnedLostHalsin</code> → <code>LearnedHalsinMissing</code> → <code>Halsin_NoTieflings</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_Conflict_SUB_Murder</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>Murder_DAStarted</code> → <code>ZevlorDied</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_Conflict_SUB_Ritual</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>Ritual_DAStarted</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_FindingDoni</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>RikkaDied</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_HarpyMeal</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>InvestigateFoundDeadKid</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_RobbedAdventurer</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>GroveChanged</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_SnakeCourt</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>MetParent</code> → <code>EnteredLair</code> → <code>KidFreed</code> → <code>ParentGone</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_Thieflings</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>EnteredHideout</code> → <code>DoneAngeredThieflings</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>DEN_VoloAdventure</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>VoloTakenAlreadyMet</code> → <code>VoloPrison</code> → <code>VoloFree</code> → <code>VoloCamp</code></li>
</ul>
</details>

### Act 1 — Swamp / Hag

<details class="quest-entry"><summary><code>HAG_HagSpawn</code><span class="q-count">11 steps</span></summary>
<ul>
<li>Trail: <code>SpokeToBrothers</code> → <code>RefusedHelpBrothers1</code> → <code>BrokeIllusion1</code> → <code>MayrinaTeleported</code> → <code>FledLair</code> → <code>EnteredLair</code> → <code>LairFight</code> → <code>HagDead</code> → <code>SavedMayrina</code> → <code>FoundWand</code> → <code>LeftHusband</code></li>
</ul>
</details>

### Act 1 — Underdark

<details class="quest-entry"><summary><code>UND_AdamantineForge</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>LearnedForgeDrow</code> → <code>EnteredForge</code> → <code>ForgeActivated</code> → <code>GolemDefeated</code> → <code>ItemPickedUp</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_AdamantineForge_SUB_Rubble</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>PassedRubble</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_BroodingSovereign</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>Start</code> → <code>AcceptedJoin</code> → <code>GlutDead</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_CaveIn</code><span class="q-count">7 steps</span></summary>
<ul>
<li>Trail: <code>MutineersAskedHelp</code> → <code>FoundExplosive</code> → <code>TrueSoulAskedHelp</code> → <code>WarnedAboutPoison</code> → <code>DuergarAskedHelp</code> → <code>CaveInCleared</code> → <code>AttackedNere</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_CaveIn_SUB_Eye</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>MutineersTaskedKillEye</code> → <code>KilledEye</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_CaveIn_SUB_Smokepowder</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>PhilomeenMapRead</code> → <code>PhilomeenGaveVial</code> → <code>PhilomeenExploded</code> → <code>Smokepowder_CaveInCleared</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_DuergarPoison</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>LearnedPoisoned</code> → <code>FoundAntidote</code> → <code>GnomeCured</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_GnomeRescue</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>ThullaAskedHelp</code> → <code>FoundPhilomeen</code> → <code>ClearedRubble</code> → <code>SidedMutineers</code> → <code>DuergarKeptGnomes</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_LoneDuergar</code><span class="q-count">7 steps</span></summary>
<ul>
<li>Trail: <code>FoundGnome</code> → <code>PersuadedBoots</code> → <code>ToldQuest</code> → <code>ShownBoots</code> → <code>GotBoots_DuergarDead</code> → <code>Report</code> → <code>GaveBoots_Reward</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_MonkAmulet</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>ToldAboutShirra</code> → <code>MadnessIncreased_AfterQuest</code> → <code>LostAmulet</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_MushroomHunter</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Trail: <code>TalkedToDerryth</code> → <code>LearnedBaelenInfo</code> → <code>FoundBaelen</code> → <code>SavedBaelen</code> → <code>TalkedToBaelen</code> → <code>ReportedToDerryth_BaelenIsAlive</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_MyconidCircle</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Trail: <code>Agreed</code> → <code>Found</code> → <code>Combat</code> → <code>DefeatedDuergar</code> → <code>ToldReward</code> → <code>GotReward</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>UND_MyconidRevenge</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>AcceptedSpaw</code> → <code>AcceptedMutineers</code> → <code>KilledNere</code> → <code>TookHeadSpaw</code> → <code>Rewarded</code></li>
</ul>
</details>

### Act 1 — Grymforge

<details class="quest-entry"><summary><code>FOR_IncompleteMasterwork</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>FindPlansBeforeJournal</code> → <code>KnowsSussurTree</code> → <code>TakeSussurBark</code> → <code>FindSussurBark</code> → <code>ObtainedDagger</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>FOR_UnfortunateGnome</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>Approach</code> → <code>Pacified</code> → <code>GnomeNotToCamp</code></li>
</ul>
</details>

### Act 1 — Githyanki Crèche

<details class="quest-entry"><summary><code>CRE_BloodOfLathander</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Trail: <code>DiscoveredGithCrest</code> → <code>DiscoveredSecretPassage</code> → <code>ArrivedAtTraps</code> → <code>GotThroughTraps</code> → <code>RetrievedLathander</code> → <code>DisarmedLathanderTrap</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>CRE_Hatchery</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>AcceptedQuestFromEsther</code> → <code>ApproachedEgg</code> → <code>AcquiredEgg</code> → <code>GaveGithyankiEgg</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>CRE_ReclaimTheNest</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>BirdsScaredOff</code></li>
</ul>
</details>

### Act 2 — Shadow-Cursed Lands

<details class="quest-entry"><summary><code>COL_FindZevlor</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>NeverFoundZevlor</code></li>
</ul>
</details>

### Act 2 — Shar questline

<details class="quest-entry"><summary><code>SHA_OldEnemy</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>FoundRaphael</code> → <code>DefeatedOrphon</code></li>
</ul>
</details>

### Act 2 — Moonrise Towers

<details class="quest-entry"><summary><code>MOO_EndKetheric</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>FacedKethericMT</code> → <code>EnteredCOL</code> → <code>FightKetheric</code> → <code>DefeatedKetheric</code> → <code>FoundKethericNetherstone</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>MOO_GnomeRescue</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>WulbrenPermaDefeatedNoBarcus</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>MOO_MintharaFate</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>MintharaLeftBehind</code></li>
</ul>
</details>

### Act 3 — Rivington / Wyrm's Crossing

<details class="quest-entry"><summary><code>WYR_OpenHandMurder</code><span class="q-count">14 steps</span></summary>
<ul>
<li>Trail: <code>LorganNoHand</code> → <code>LorganPoisoned</code> → <code>BrilgorDidntDoIt</code> → <code>LorganKilledTunnels</code> → <code>FindKeyOrigin</code> → <code>EnterCaves</code> → <code>DaggerFoundAfterKey</code> → <code>FindChappelUnderground</code> → <code>LorganHidesRefugees</code> → <code>FollowTheKeyBecauseFlophouse</code> → <code>FollowTheKeyBecauseKey</code> → <code>GettingTargetList</code> → <code>TalkerValeriaDagger_Flophouse</code> → <code>WYR_ConvinceAnnoyingElephant_GoToDevella</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>WYR_Posthouse</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>TressymGone</code> → <code>TookLetter</code> → <code>KnowsAboutMissingLetters</code> → <code>HandedInLetterBigReward</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>WYR_RedDwarfMother</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>FoundCorpse_DidntKnow</code> → <code>LearnedMissing</code> → <code>FoundCorpse_Knew</code> → <code>Rewarded</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>WYR_Windmill</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>MindflayerHunts</code></li>
</ul>
</details>

### Act 3 — Upper City

<details class="quest-entry"><summary><code>SCL_CursedFist</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>MetArt</code> → <code>FoundNote</code> → <code>FoundLute</code> → <code>HalsinAlreadyDead</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>SCL_Inquisition</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>ReadPottersNote</code> → <code>SearchLastLight</code> → <code>DiscoveredLastLight</code> → <code>InvestigateMason</code> → <code>FoundMasonResistance</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>SCL_LiftingTheCurse</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>OliverNotFound</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>SCL_ServantOfTheRaven</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>AgreedToHelp</code> → <code>FoundLedger</code> → <code>ForgaveMadeline</code></li>
</ul>
</details>

### Act 3 — City (misc)

<details class="quest-entry"><summary><code>PLA_PaladinsOfTyr</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>TakeHead</code> → <code>MetAnders_AgreedHelp</code> → <code>DeliverHead</code> → <code>KilledKarlach_Reward</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>PLA_StuckHalfElf</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>Start</code> → <code>Dead</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>PLA_ZhentShipment</code><span class="q-count">6 steps</span></summary>
<ul>
<li>Trail: <code>FoundOrders</code> → <code>NoticedStruggle_Cave</code> → <code>GnollGone_OneDied_NotMet</code> → <code>GotPassword</code> → <code>AgentsDied_NoSave</code> → <code>ChestOpened</code></li>
</ul>
</details>

### Main quest (cross-act)

<details class="quest-entry"><summary><code>GLO_ArtefactDiscovery</code><span class="q-count">11 steps</span></summary>
<ul>
<li>Trail: <code>GoblinCampGoblinKing</code> → <code>AtGithChokepoint</code> → <code>InGithCreche</code> → <code>GainedAccessToCreche</code> → <code>EnteredCaptainsOffice</code> → <code>TalkedToCaptain</code> → <code>KeptArtefact</code> → <code>SpokenToVlaakith</code> → <code>EnteredAstralPrism</code> → <code>SpokenToDreamVisitor_Save</code> → <code>LeftPrisonSavedDaisy</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Moonrise</code><span class="q-count">16 steps</span></summary>
<ul>
<li>Trail: <code>EnteredSCL_KnowsMT_HadDream</code> → <code>DriderCaravanUpdate</code> → <code>KnowHaven_HarperScouts</code> → <code>VisitedHaven_Reach</code> → <code>JaheiraKillKetheric_Reach</code> → <code>InfiltratorStart_Reach</code> → <code>ProtectedIsobel_Reach</code> → <code>TookMoonlantern_Parent</code> → <code>FoundBalthazar</code> → <code>BalthazarDied_Parent</code> → <code>FreedNS_Parent</code> → <code>FoundMOO</code> → <code>FoundMOO_Assault</code> → <code>PassedMTCheckpoint</code> → <code>AssaultedMT</code> → <code>FacedKethericMT</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Moonrise_SUB_Caravan</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>CaravanNoLyre</code> → <code>CaravanGotLyre</code> → <code>MeetDrider_Summoned</code> → <code>DefeatedHarpers</code> → <code>DriderUnavailable</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Moonrise_SUB_MarcusCapture</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>BetrayMarcus</code> → <code>MarcusBrief</code> → <code>SavedIsobel</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Moonrise_SUB_Relic</code><span class="q-count">15 steps</span></summary>
<ul>
<li>Trail: <code>Relic_FoundEntrance</code> → <code>Relic_MetNecromancer</code> → <code>Relic_StartedTrials</code> → <code>Relic_FoundGemOrthon</code> → <code>Relic_FoundGem</code> → <code>Relic_FoundGemTrials</code> → <code>Relic_FoundGemTrials2</code> → <code>AcquiredAllGems_UpperAltar</code> → <code>ActivatedElevator</code> → <code>ActivatedElevator_HasGems</code> → <code>Relic_SeenSecondAltar</code> → <code>Relic_OpenedPath</code> → <code>Relic_EnterShadowfell</code> → <code>Relic_NecromancerFight</code> → <code>Relic_FreedNS</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Moonrise_SUB_Shadowcurse</code><span class="q-count">10 steps</span></summary>
<ul>
<li>Trail: <code>ExperiencedShadowcurse_KnowsNereLantern</code> → <code>MetDrider</code> → <code>Protection_HelpedHarperScouts</code> → <code>Protection_VisitedHaven</code> → <code>IsobelProtection</code> → <code>GotSeluneProtection</code> → <code>TakeMoonlantern_Betrayed</code> → <code>TakeMoonlantern</code> → <code>InspectMoonlantern</code> → <code>ReleasedPixie</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_PathToMoonrise</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>HalsinToldPath</code> → <code>ToldMoonrise_Daisy</code> → <code>ReachedSCL</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_PathToMoonrise_SUB_MountainPath</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>Mountain_HalsinToldPath</code> → <code>Mountain_ReachedSCL_Grymforge</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_PathToMoonrise_SUB_UnderdarkPath</code><span class="q-count">5 steps</span></summary>
<ul>
<li>Trail: <code>Underdark_EnteredUnderdark</code> → <code>Underdark_HalsinToldPath</code> → <code>Underdark_LearnedSearch</code> → <code>Underdark_DuergarToldAboutCurse</code> → <code>Underdark_ReachedSCL_KethericCity</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_PrinceOrpheus</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>DecipherBook1</code> → <code>DecipherBook3</code> → <code>DecipheredAllBooks</code> → <code>DiscoveredOrpheusIsInArtefact</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Tadpole</code><span class="q-count">18 steps</span></summary>
<ul>
<li>Trail: <code>WokeAtCrash</code> → <code>LaezelJoined</code> → <code>LearnedGroveSituation</code> → <code>LearnedApprentice</code> → <code>LearnedPriestess</code> → <code>LearnedEthel</code> → <code>SawTrueSoulMeldOrTadpole</code> → <code>LearnedHalsin</code> → <code>ApprenticeDiedEarly</code> → <code>EthelDiedEarly</code> → <code>PriestessImprisoned_ParentCompletion</code> → <code>LearnedSociety</code> → <code>ToldAboutRing_ParentCompletion</code> → <code>StartedInfiltration</code> → <code>UsedPurifier_ParentCompletion</code> → <code>HalsinDiedEarly</code> → <code>Tadpole_PointOfNoReturn</code> → <code>SeenChosenThree</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Tadpole_SUB_Apprentice</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>LearnedAboutNettie</code> → <code>ApprenticeDied_BeforeTadpoled</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Tadpole_SUB_Creche</code><span class="q-count">11 steps</span></summary>
<ul>
<li>Trail: <code>LaezelToldCreche</code> → <code>FoundZorru_LaezelAtCamp</code> → <code>TieflingToldCreche</code> → <code>FoundPatrol_KnewAboutPatrol</code> → <code>LearnedCreche</code> → <code>ReachedCreche</code> → <code>ReachedCrecheRegionWithClues</code> → <code>ReachedCreche_BetrayedByVlaakith</code> → <code>UseDevice</code> → <code>FoundInfirmary</code> → <code>UsedPurifier</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Tadpole_SUB_Druid</code><span class="q-count">3 steps</span></summary>
<ul>
<li>Trail: <code>LearnedLostHalsin</code> → <code>LearnedBear</code> → <code>Betrayal_DruidDead_Halsin</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Tadpole_SUB_Hag</code><span class="q-count">2 steps</span></summary>
<ul>
<li>Trail: <code>ToldEthelAboutTadpoleInGrove</code> → <code>HagHostile</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Tadpole_SUB_Priestess</code><span class="q-count">4 steps</span></summary>
<ul>
<li>Trail: <code>LearnPriestess</code> → <code>GotBranded</code> → <code>ReachedQuarters</code> → <code>PriestessImprisoned</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_Tadpole_SUB_Society</code><span class="q-count">11 steps</span></summary>
<ul>
<li>Trail: <code>AskedToCheck</code> → <code>ToldAboutNullify</code> → <code>AskedToFind</code> → <code>LearnedCleric</code> → <code>FoundSpore</code> → <code>FoundTongue</code> → <code>MadeExtract</code> → <code>LostOne</code> → <code>AskedToDrink</code> → <code>ToldAboutRing</code> → <code>GotTheRing</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>GLO_UnleashPowers</code><span class="q-count">7 steps</span></summary>
<ul>
<li>Trail: <code>FoundTadpole</code> → <code>UsedIllithidPersuasion</code> → <code>FindTadpole</code> → <code>DaisyAskedConsume</code> → <code>FindMoreTadpoles</code> → <code>UseAstralTadpole</code> → <code>UsedAstralTadpole</code></li>
</ul>
</details>

### Companion questlines

<details class="quest-entry"><summary><code>ORI_Avatar_ShadowHeart_SUB_ChosenOfShar</code><span class="q-count">10 steps</span></summary>
<ul>
<li>Trail: <code>JusticiarHint_Halsin_Start</code> → <code>JusticiarHint_TempleApproach_Start</code> → <code>FoundSharTemple_Alt</code> → <code>SharSpoke</code> → <code>FoundTrials</code> → <code>LootedSpearOfNight_Communion</code> → <code>CompletedTrials</code> → <code>EnteredShadowfell_Balthazar</code> → <code>BalthazarFight</code> → <code>Nightsong_Spared</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Karlach</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>KarlachPermanentlyDies</code></li>
</ul>
</details>

<details class="quest-entry"><summary><code>ORI_COM_Karlach_SUB_ForgingOfTheHeart</code><span class="q-count">1 step</span></summary>
<ul>
<li>Trail: <code>ForgingOfTheHeart_KarlachPermanentlyDies</code></li>
</ul>
</details>
